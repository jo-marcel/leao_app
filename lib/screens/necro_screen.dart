import 'package:flutter/material.dart';

class NecroScreen extends StatelessWidget {
  const NecroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("🕊️ Nécrologie - Hommages aux anciens"),
      ),
    );
  }
}
